import { RouteSkeleton } from "@/components/shared/route-skeleton";

// Shown by Next.js automatically while the dashboard page loads.
export default function Loading() {
  return <RouteSkeleton />;
}
