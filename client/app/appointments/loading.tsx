// Shown by Next.js automatically while the appointments page is loading.
// The hero and skeleton cards give a visual preview of what's coming.

import { RouteSkeleton } from "@/components/shared/route-skeleton";

export default function Loading() {
  return <RouteSkeleton />;
}
