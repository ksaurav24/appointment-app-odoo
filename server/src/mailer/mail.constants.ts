// The literal queue name used by the @Processor decorator. Must match the
// value in env (MAIL_QUEUE_NAME); kept as a constant because the decorator
// requires a static string.
export const MAIL_QUEUE_NAME = 'mail';
